name: Release VSCode Extension

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-extension

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: vscode-extension/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Verify tag matches package.json version
        run: |
          PKG_VERSION="v$(node -p "require('./package.json').version")"
          TAG="${GITHUB_REF_NAME}"
          if [ "$PKG_VERSION" != "$TAG" ]; then
            echo "::error::Tag $TAG 与 package.json 版本 $PKG_VERSION 不一致，请先更新版本号再打 tag"
            exit 1
          fi

      - name: Build & Package (.vsix)
        run: npx vsce package --no-dependencies -o gte-editor-${GITHUB_REF_NAME}.vsix

      # VSCE_PAT secret 存在时才发布到 Marketplace；PR 标记版本自动跳过
      - name: Publish to Marketplace (optional)
        if: ${{ env.VSCE_PAT != '' && !contains(github.ref_name, '-') }}
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: npx vsce publish --no-dependencies
        continue-on-error: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: vscode-extension/*.vsix
